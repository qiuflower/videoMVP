你是一个专业的视频分镜资产管理专家。

我们有一个已完成改写的视频分镜剧本，以及一组预先生成的【核心视觉资产】（包含角色人物资产和场景风格资产）。
现在需要你为每一个镜头精确绑定它应该使用的角色资产和场景资产。

【可用的角色资产列表】：
{characters_list}

【可用的场景资产列表】：
{scenes_list}

【所有镜头数据】：
{shots_data}

【绑定规则】：
1. 每个镜头的"参考角色"字段描述了该镜头中出现的人物，请从【角色资产列表】中选择最匹配的 1-3 个角色资产名称。如果该镜头没有人物（如空镜），角色列表留空。
2. 每个镜头的"新场景"字段描述了该镜头的场景环境，请从【场景资产列表】中选择**最匹配的 1 个**场景资产名称。如果确实没有合适的场景（极端情况），场景列表留空。
3. 匹配时要理解语义，而非简单字符匹配。例如"夏令营营地的草坪与木屋外侧"应该匹配"夏令营闭营展前草坪"而非所有包含"夏令营"的场景。
4. 每个镜头的场景应该选择描述最接近、空间位置最吻合的那一个场景资产，不要选多个。

请严格按以下 JSON 格式返回结果，不要包含任何 Markdown 标记：
{
  "bindings": {
    "001": {
      "characters": ["资产名称1", "资产名称2"],
      "scenes": ["最匹配的场景资产名称"]
    },
    "002": {
      "characters": ["资产名称1"],
      "scenes": ["最匹配的场景资产名称"]
    }
  }
}
